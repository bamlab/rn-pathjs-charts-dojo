// @flow

import { StyleSheet } from 'react-native';
import theme from 'RNPathjsChartsDojo/src/theme';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#F86422',
    paddingTop: theme.grid.x2,
  },
});
